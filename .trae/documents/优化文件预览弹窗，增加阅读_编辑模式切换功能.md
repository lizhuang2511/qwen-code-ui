我将修改 `d:\giteecode\qwencode5\crates\mdConvert.py` 文件，通过优化 HTML 模板和转换逻辑来实现文件预览弹窗的“阅读模式”与“编辑模式”切换功能。

### 计划修改内容：

1.  **引入必要模块**：
    *   导入 `html` 模块，用于对插入到 `textarea` 的源代码进行转义，防止 HTML 标签破坏页面结构。

2.  **更新 `myhtml` 模板**：
    *   **添加切换按钮**：在页面右上角添加一个悬浮按钮，用于在“阅读模式”和“编辑模式”之间切换。
    *   **添加编辑容器**：增加一个 `<textarea>` 元素用于显示文件的原始代码，初始状态为隐藏。
    *   **添加 CSS 样式**：为切换按钮和编辑区域添加样式，确保编辑器全屏显示且字体为等宽字体。
    *   **添加 JavaScript 逻辑**：实现点击按钮时控制预览视图 (`.markdown-body`) 和编辑视图 (`textarea`) 的显示/隐藏切换，并动态更新按钮文本。
    *   **增加占位符**：将模板的格式化参数从 2 个（CSS, HTML）增加到 3 个（CSS, HTML, 原始内容）。

3.  **优化 `md2html` 函数**：
    *   **代码文件支持**：根据 `in_file` 的扩展名判断文件类型。如果不是 `.md` 文件，则在渲染时自动将内容包裹在 Markdown 代码块中（例如 \`\`\`python ... \`\`\`），以便在阅读模式下获得语法高亮体验。
    *   **原始内容处理**：保留原始文本用于编辑模式，并使用 `html.escape()` 进行转义。
    *   **参数传递**：将处理后的 HTML 和转义后的原始内容传递给新的 `myhtml` 模板。

4.  **同步更新 `md2html0` 函数**：
    *   保持与 `myhtml` 模板参数的一致性，增加原始内容的转义和传递。

### 预期效果：
*   **默认状态**：打开文件时显示渲染后的 HTML（阅读模式）。
*   **交互**：点击右上角“编辑模式”按钮，界面切换为显示原始内容的文本框，可进行复制或查看源码。
*   **代码文件**：非 Markdown 文件（如 .py, .js）在阅读模式下将显示为高亮的代码块，而不是纯文本。

请确认是否执行此计划。