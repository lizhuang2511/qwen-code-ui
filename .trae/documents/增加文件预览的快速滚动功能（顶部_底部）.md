I will modify the code to align with the user's latest request: "Can scroll is enough, no need for shortcut buttons".

I will:
1.  **Remove Scroll Buttons from Backend**: Edit `d:\giteecode\qwencode5\crates\mdConvert.py` to remove the "Scroll to Top" and "Scroll to Bottom" buttons and their associated JavaScript functions, as the user indicated they are not needed.
2.  **Remove Scroll Buttons from Frontend**: Edit `d:\giteecode\qwencode5\frontend\src\components\common\FileContentViewer.tsx` to remove the scroll buttons from the toolbar that were added in the previous turn.
3.  **Ensure Scrolling Works**: Verify (by code inspection) that both Preview and Edit modes have `overflow: auto` configured correctly so that standard mouse wheel scrolling works.

**Detailed Changes:**

*   **`crates/mdConvert.py`**:
    *   Remove `<button id="scroll-top-btn" ...>` and `<button id="scroll-bottom-btn" ...>`.
    *   Remove CSS for `.scroll-btn`, `#scroll-top-btn`, `#scroll-bottom-btn`.
    *   Remove JS functions `scrollToTop` and `scrollToBottom`.
*   **`frontend/src/components/common/FileContentViewer.tsx`**:
    *   Remove the `ArrowUp` / `ArrowDown` buttons from the `DialogTitle` (lines 480-502).
    *   Remove `handleScrollToTop` and `handleScrollToBottom` functions.
    *   Remove `useRef` for `scrollContainerRef`.
    *   Remove `ref={scrollContainerRef}` from the wrapper div.

This ensures the interface remains clean ("No need for shortcut buttons") while maintaining standard scrolling functionality for both reading and editing.