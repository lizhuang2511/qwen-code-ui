## 问题分析
从错误信息看，主要问题是 "Qwen CLI not found at path: qwen"。这表明系统无法找到 `qwen` 命令行工具。

## 修复方案

### 1. 修改 CLI 路径检测逻辑
- 在 `qwen_python_wrapper.py` 中添加自动检测 Qwen CLI 路径的功能
- 尝试多个可能的路径（系统 PATH、常见安装位置）
- 提供更详细的错误信息

### 2. 增强错误处理
- 区分不同类型的错误（CLI 未安装、CLI 路径错误、认证问题等）
- 提供更具体的解决方案提示

### 3. 更新示例代码
- 修改 `example_usage.py` 以处理 CLI 未找到的情况
- 添加更友好的用户提示

### 4. 具体修改内容

#### 修改 `qwen_python_wrapper.py`:
1. 在 `__init__` 方法中添加 CLI 路径自动检测
2. 添加 `_find_cli_path()` 方法
3. 改进错误消息

#### 修改 `example_usage.py`:
1. 添加 CLI 可用性检查
2. 提供更详细的错误处理
3. 添加安装指导提示

## 预期结果
修复后，代码将能够：
- 自动检测 Qwen CLI 的安装位置
- 提供清晰的安装和配置指导
- 更好地处理各种错误情况