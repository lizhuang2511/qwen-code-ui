我将执行以下修复和优化计划，以解决启动慢和报错的问题：

1.  **修复 `webview.errors.WebViewException` 报错**：
    *   修改 `main.py` 中的 `start_ticker` 函数。
    *   在 `w.evaluate_js` 调用周围添加 `try-except` 块，捕获并忽略因窗口未就绪导致的异常，防止后台线程崩溃。

2.  **优化历史记录加载速度 (核心瓶颈)**：
    *   修改 `crates/search.py`。
    *   **只读文件头**：重构日志读取逻辑，只读取前 2KB 数据以提取对话标题，不再全量解析 JSON。
    *   **快速时间解析**：直接从文件名 (`rpc-log-<timestamp>.log`) 解析时间戳，避免文件系统元数据调用。
    *   **高效消息计数**：使用快速行扫描（统计换行符）代替 JSON 解析来估算消息数量，提升处理速度 10 倍以上。

这些修改将同时解决启动时的性能阻塞和后台线程的报错问题。