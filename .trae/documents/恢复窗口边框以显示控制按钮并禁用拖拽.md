我将优化关闭功能的实现，确保点击前端自定义关闭按钮时，也能触发“是否保存对话记录”的提示，并正确处理窗口关闭逻辑。

**计划执行步骤**:

1.  **后端 - 增强 `Api` 类 (`crates/backend/api.py`)**:
    *   添加 `maximize_window()` 方法。
    *   修改 `quit_app()` 方法：增加对 `on_close_callback` 的检查。如果设置了回调且回调返回 `False`（取消），则中止关闭；否则继续执行 `destroy()`。
    *   添加 `set_close_handler(callback)` 方法，用于注册回调。

2.  **后端 - 入口文件 (`main.py` 和 `start.py`)**:
    *   **实例化 API**: 将 `Api()` 的实例化提前，以便配置。
    *   **绑定关闭逻辑**: 将现有的 `on_closing` 函数注册给 `api.set_close_handler`，确保前端点击关闭按钮时执行相同的保存确认逻辑。
    *   **窗口配置**: 设置 `frameless=True` (无边框) 和 `easy_drag=False` (禁用全窗拖拽)。

3.  **前端 - 接口定义 (`src/lib/api.ts` 和 `src/lib/webApi.ts`)**:
    *   添加 `maximize_window` 方法定义。

4.  **前端 - 自定义标题栏 (`src/components/layout/CustomTitleBar.tsx`)**:
    *   允许在 `pywebview` 环境显示。
    *   添加 CSS 拖拽区域 (`-webkit-app-region: drag`)。
    *   连接新的 `api.maximize_window()`。
    *   连接 `api.quit_app()` (它现在会触发后端的回调)。

确认后，我将开始执行这些更改。