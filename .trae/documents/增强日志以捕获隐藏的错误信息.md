**分析：**
1.  **错误现状**：日志显示 `[SESSION] 后端: 工具调用失败详情: null`，这意味着 Qwen CLI 返回了 `status=failed`，但没有在 `result` 字段中提供错误信息。
2.  **可能原因**：
    *   Qwen CLI 的报错信息可能被输出到了 **STDERR**（标准错误流），而当前代码只将其发送到了前端事件，没有打印在后端控制台。
    *   或者 Qwen CLI 返回的 JSON 数据在适配器日志中被截断（显示为 `statu...`），导致我们在日志中没看到完整的原始数据。
3.  **验证结论**：根据之前的 Rust 代码分析，架构是正确的（CLI 负责文件写入）。现在的关键是**看见** CLI 到底报了什么错。

**执行计划：**

1.  **修改 `crates/session.py`**：
    *   在 `read_stderr` 函数中增加 `print(f"[SESSION-STDERR] {ln}")`，将 CLI 的错误输出直接打印到控制台。
    *   在 `read_stdout` 处理 `tool_call_update` 时，强制打印完整的原始 JSON 行 `print(f"[SESSION-RAW-UPDATE] {ln}")`，防止日志截断掩盖真相。

2.  **预期结果**：
    *   执行后，当您再次点击 "Allow" 时，我们应该能在终端看到类似 `[SESSION-STDERR] Permission denied` 或具体的 Python/Node 报错堆栈。
    *   这将直接证实是否为权限问题（需要管理员启动）或路径配置问题。

我将立即添加这些调试打印。