I will modify `frontend/src/components/common/DirectoryPanel.tsx` to enhance the visual feedback when hovering over files and folders.

### 1. Update `frontend/src/components/common/DirectoryPanel.tsx`
- **Enhance Hover Style**: Change the CSS class for the tree node container from `hover:bg-muted/50` to `hover:bg-accent hover:text-accent-foreground`.
- **Effect**: This will provide a more distinct and standard highlighting effect (background color change) when the user moves their mouse over any file or folder in the directory tree, making it clearer which item is currently being targeted.

This change directly addresses the request to optimize the highlighting of files/folders on hover.
