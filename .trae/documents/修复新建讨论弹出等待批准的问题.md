我已找到导致“新建讨论弹出等待批准”的原因。

**问题分析：**
在 `crates/session.py` 文件中，后端会在会话启动时启动一个后台线程检测 CLI 进程的输出。如果在 0.5 秒内没有收到任何输出（或者在模拟模式下），代码会触发一个名为 "init" 的伪工具调用，并发送 `acp-permission-request` 事件。前端接收到这个请求后，就会显示“等待批准”的对话框，其中的 "Other" 对应未知的工具名称 "init"。

这部分逻辑似乎是为了处理 CLI 启动缓慢或无输出的情况，但它错误地阻断了用户的正常对话流程。

**修复计划：**
我将修改 `crates/session.py`，移除或注释掉触发 "init" 工具调用和权限请求的代码。这将涉及以下几个位置：

1. 无效工作目录的模拟块（约第 121-122 行）。
2. Qwen 后端的 fallback 逻辑（约第 176-177 行）。
3. 通用 CLI 的 fallback 逻辑（约第 228-229 行）。
4. CLI 未找到的模拟块（约第 256-257 行）。

修改后，新建讨论将不再弹出批准窗口，而是直接进入就绪状态，允许用户直接开始对话。

**具体步骤：**

1. 编辑 `crates/session.py`。
2. 删除上述位置中发射 `acp-session-update` (toolCallId: "init") 和 `acp-permission-request` 的代码行。

