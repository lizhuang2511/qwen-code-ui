收到，既然允许使用 `try...except`，我可以采用更简洁、高效的方案，无需引入多线程。

**计划概要**：
1.  **修改文件**：`crates/session.py`。
2.  **核心逻辑**：
    *   在 `read_stdout` 函数内部，针对 `QwenProcess`（AI 进程）实现一个带超时的生成器。
    *   利用 `queue.get(timeout=0.05)` 实现非阻塞读取。
    *   当读取超时（0.05秒无输出）时，捕获 `Empty` 异常并立即调用 `flush_ui_buffer()`，强制刷新缓冲区。这直接解决了“漏发”和“延迟”问题。
    *   对于普通进程，保持原有逻辑，但将刷新时间间隔从 0.1s 调整为 0.05s。
3.  **优势**：
    *   单线程逻辑，无需锁，避免竞态条件。
    *   精确控制超时时间。
    *   代码结构改动最小，复用现有的解析逻辑。

我将立即开始修改 `crates/session.py`。