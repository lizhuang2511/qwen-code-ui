明白，将不强制开启 TUI 模式，而是直接在当前环境下严格按照您描述的步骤发送指令和按键序列，以测试 Qwen CLI 的反应。

### 最终计划内容

**1. 创建测试脚本 `test_approval_mode_interaction.py`**
   - **查找可执行文件**：自动定位 `qwen` 或 `qwencodecli`。
   - **执行测试流程**：
     1.  **新建 Qwen 进程**：使用 `subprocess` 启动 CLI。
     2.  **发送命令**：向标准输入写入 `/approval-mode`。
     3.  **发送按键**：依次写入 `Down Arrow` (`\x1b[B`) 和 `Enter` (`\n`)。
     4.  **发送查询**：写入文本 `"你现在什么模式\n"`。
   - **结果输出**：读取并打印 CLI 的所有回复，以验证它是切换了模式，还是将上述输入识别为了普通文本错误。

此脚本将直接复现您描述的操作步骤。