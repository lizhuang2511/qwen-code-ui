The frontend console logs reveal two key issues causing the "stuck in running" state:

1. **Dialog State Error**: `use-controllable-state.tsx:42 Dialog is changing from uncontrolled to controlled.` This indicates a React state management issue, likely in the permission dialog or tool call display components. This can cause UI elements to not render or update correctly.

2. **Tool Call Update Race Condition**: `useConversationEvents.ts:615 Tool call not found for update: null` This is the primary cause. The frontend is receiving `tool_call_update` events with a `toolCallId` of `null`. This happens when the backend (`session.py`) emits an `acp-session-update` event but the `toolCallId` field is missing or incorrectly extracted/passed.

**Fix Plan:**

1. **Investigate Backend (`crates/session.py`)**: Find where `tool_call_update` events are emitted. Ensure that the `toolCallId` is correctly extracted from the parsed message and included in the event payload. The log `[SESSION] Processing tool_call_update: id=None status=None` from a previous run confirms the backend is receiving `toolCallId` as `None`.

2. **Investigate Parser (`crates/parsers/qwen.py`)**: Ensure that the parser correctly extracts the `toolCallId` from the `session/update` message and passes it up to `session.py`. The structure is likely \`{"params": {

