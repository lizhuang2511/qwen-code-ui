## 抖音/快手短视频提示词（30–45秒｜9:16竖屏）

### Hyperframes（HTML 转视频）专用提示词（可直接粘贴）

你是前端动效设计师 + 信息流短视频编导。请输出一份**单文件 HTML**（内联 CSS，可少量原生 JS 但不依赖任何外部资源/CDN），用于 Hyperframes 直接渲染导出视频。

硬性要求：
- 画布：1080×1920（9:16），背景深色科技风，文字高对比清晰
- 时长：40s（允许 ±2s），帧率按工具默认即可
- 字体：优先系统黑体/思源黑体风格（不要引用在线字体）
- 动画：只用 CSS keyframes（推荐），通过 `animation-delay` 精准对齐时间轴
- 结构：用 6 个场景（scene1~scene6），每个场景在自己的时间段淡入/淡出；场景之间不要硬切
- 画面内容：全部中文；不出现夸张承诺（如“全网最强/永久免费”）
- 必须出现并强调：痛点、用途、功能覆盖、强大性、免费使用（学习交流）、作者联系方式

内容要点（必须覆盖）：
- 产品名：机械设计软件集成平台 v2.0（机械设计程序）
- 一句话定位：一站式机械设计计算工具箱
- 痛点：公式多、工具分散、手算慢且易错、结果不易留档
- 功能覆盖（字幕轮播即可）：齿轮/涡轮与强度、轴承选型与寿命、螺栓/键/销/花键/过盈连接校核、轴挠度与疲劳、V 带/链传动、弹簧设计、公差配合查询
- 强大性：可视化输入 → 一键计算/校核；部分模块支持导出 PDF 报告；多模块可同时打开；统一启动更稳定
- 结尾收尾三行固定文案：
  - 机械设计软件集成平台 v2.0
  - 免费使用（学习交流）
  - 作者 lizhuang：15736965216

时间轴（必须按这个节奏做）：
- 0–3s（scene1 钩子）：大字痛点钩子 + 背景出现“手册/Excel/公式”碎片化元素
- 3–8s（scene2 亮相）：产品名 + “一站式机械设计计算工具箱”
- 8–20s（scene3 模块集成）：左侧树形分类拟态 UI + 右侧模块弹窗卡片连续弹出（每 1s 一张）
- 20–30s（scene4 一键出结果）：表格/结果卡片“合格/校核通过”拟态展示 + 关键句“更快、更不容易算错”
- 30–35s（scene5 留档）：PDF 文件卡片/下载箭头动效 + 文案“结果可留档（PDF 报告）”
- 35–40s（scene6 收尾）：三行固定文案 + 轻微呼吸光效

视觉规范（请在 HTML 里落地）：
- 颜色：#0B1020 背景；点缀 #4F7CFF / #22D3EE；正文 #EAF0FF；弱文字 #9AA7C7
- 排版：标题 72–86px；副标题 40–48px；要点 34–40px；行距 1.2–1.35
- 安全边距：四周至少 80px
- 动效：淡入淡出（300–500ms）+ 轻微上移（12–20px）+ 关键字高亮描边/底色条

输出格式要求：
- 只输出一份可运行的 HTML（包含 `<style>`，必要时包含 `<script>`）
- HTML 内部用注入式文本，不要写“此处替换”
- 所有场景元素都在同一页面内，通过动画控制显示

---

### 一键生成视频提示词（推荐）

生成一条 9:16 竖屏、40 秒左右的产品功能宣传短视频，节奏快、信息密度高。主题：机械设计软件集成平台 v2.0（机械设计程序），主打“一站式机械设计计算工具箱”。画面风格：真实屏幕录制质感 + 少量工程师工作场景 B-roll，UI 清晰、文字锐利、科技感但不花哨。配乐：轻快科技感节奏（鼓点清晰），关键点击配合轻微音效。

必须出现并强调的卖点：
- 现状痛点：机械设计计算公式多，翻手册 + Excel/工具分散，手算慢且易错，结果不易留档
- 解决方案：一个窗口集成多模块，树形分类，双击即启动模块
- 功能覆盖示例（字幕轮播）：齿轮/涡轮与强度、轴承选型与寿命、螺栓/键/销/花键/过盈连接校核、轴挠度与疲劳、V带/链传动、弹簧设计、公差配合查询等
- 强大性：可视化输入 → 一键计算/校核；部分模块支持导出 PDF 报告；多模块可同时打开；统一启动更稳定
- 结尾必须给出：免费使用（学习交流）；作者与联系方式：lizhuang 15736965216

镜头结构（按时间）：
- 0–3s：工程师在电脑前焦虑翻手册/Excel，字幕大字钩子：“机械设计计算，还在翻手册+Excel？”
- 3–8s：切到软件主界面清晰展示，字幕：“一个窗口，集成机械设计常用计算”
- 8–20s：鼠标在树形分类上快速点选/双击，连续弹出多个模块窗口（轴承/齿轮/螺栓/V带/弹簧等），每个停留 1 秒，字幕轮播模块名
- 20–30s：展示“填参数→立刻出结果/校核”的结果界面（数据表/结论），字幕：“更快、更不容易算错”
- 30–35s：展示“导出 PDF 报告/保存结果”的画面，字幕：“结果可留档（PDF 报告）”
- 35–40s：回到主界面 + 品牌收尾，字幕三行依次出现：
  - “机械设计软件集成平台 v2.0”
  - “免费使用（学习交流）”
  - “作者 lizhuang：15736965216”

字幕与转场要求：
- 全程叠加中文动态字幕，字体简洁有力量（黑体/思源黑体风格）
- 转场干净利落，避免花哨特效
- 画面必须突出“树形分类 + 双击打开模块 + 结果界面 + PDF 导出 + 联系方式”

---

### 口播台词（约40秒）

- 0–3s：机械设计计算，还在翻手册、套公式、做 Excel 吗？
- 3–8s：试试这个——机械设计软件集成平台 v2.0。
- 8–20s：一个窗口，按分类双击就能开模块：齿轮、轴承、连接校核、轴计算、V 带链传动、弹簧、公差配合查询……
- 20–30s：把参数填进去，一键计算和校核，速度更快，也更不容易算错。
- 30–35s：部分模块还能导出 PDF 报告，结果直接留档。
- 35–40s：现在免费使用，作者 lizhuang，联系方式 15736965216。

---

### 屏幕大字字幕（更抖音化）

- 别再手算了
- 机械设计计算工具箱
- 双击开模块 / 一站集成
- 填参数 → 一键计算/校核
- PDF 报告可留档
- 免费使用（学习交流）
- lizhuang：15736965216
